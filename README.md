# 8-queens-hill-climbing
8-Queens Problem solved using the Steepest-Ascent Hill Climbing algorithm (Local Search).  Heuristic function: h(n) = number of pairs of queens attacking each other (conflicts in rows and diagonals).  Objective: Minimize h(n) iteratively until reaching h(n) = 0, which represents a valid solution with no conflicts.
